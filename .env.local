# Vercel and React/Vite/Next.js/etc. require the VITE_ or NEXT_PUBLIC_ prefix 
# for variables to be exposed to the browser.
# Assuming you are using Vite, use VITE_ prefix:

VITE_GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"

# Ensure you replace "YOUR_GEMINI_API_KEY_HERE" with the actual, unrestricted key you generated.
